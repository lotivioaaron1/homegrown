<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off.

         Parented on Theme.Black, not Theme.Light, even in the light-mode
         configuration: the launch screen is a fixed dark scene in both modes so
         it meets splash_screen.dart's near-black first frame without a flash.
         The light parent is what used to paint the window white and the status
         bar icons dark, which is exactly the wrong pair on this ground. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>

        <!-- Android 12+ (API 31) draws its own launch screen and ignores
             windowBackground for it, so the colour has to be stated again
             here; the three attributes below are unknown to older devices and
             quietly ignored, which is why they can sit in the base theme
             rather than a values-v31 copy.

             The icon background matches the splash background on purpose: the
             system masks the icon to a circle of that colour, and matching it
             makes the plate disappear so the HG mark reads as floating rather
             than sitting on a visible disc. -->
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Stated explicitly rather than left as ?android:colorBackground,
             which resolves white in light mode. This window sits behind Flutter
             and shows through for a frame or two during startup, so a white one
             here reintroduces the flash the rest of this file removes. -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
